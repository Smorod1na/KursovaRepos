<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Scripts.Render("~/bundles/modernizr")
    @Styles.Render("~/Content/css")
    @Styles.Render("~/Content/MyCss")
    @*<link href="~/Content/MyStyleFirst.css"/ rel="stylesheet">*@
</head>
<body>
    <div class="navbar navbar-dark bg-dark">
        
            <div class="navbar-header">
             
                @Html.ActionLink("Manager", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
           
                @Html.Partial("_LoginPartial")
          

       
    </div>

    <div class="container body-content">
        @RenderBody()
        <hr />
        @*<footer>
                <p>&copy; @DateTime.Now.Year - My ASP.NET Application</p>
            </footer>*@
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>




@*<div class="container-fluid">
    <nav class="navbar navbar-dark bg-dark">

        <div class="navbar-header">

            @Html.ActionLink("Home controller", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
        </div>
        @if (Request.IsAuthenticated)
        {
            @Html.Partial("LogOff")

        }
        else
        {
            <ul class="nav navbar-nav d-flex flex-row">
                <li>
                    @Html.Partial("Register")

                </li>

                <li>
                    @Html.Partial("Login")
                </li>
            </ul>
        }

    </nav>
</div>*@